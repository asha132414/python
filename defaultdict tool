from collections import defaultdict

n, m = map(int, input().split())

groupA = defaultdict(list)

# Read group A words and store their positions (1-indexed)
for i in range(1, n + 1):
    word = input().strip()
    groupA[word].append(i)

# Read group B words and print positions or -1
for _ in range(m):
    word = input().strip()
    if word in groupA:
        print(*groupA[word])
    else:
        print(-1)
